Prior findings:
- [major] `sys.path` bootstrap: addressed. Plan v1 keeps the source-checkout bootstrap in all four examples and verifies direct example execution without install.
- [minor] old `run_j0126_eval` surface: addressed. Plan v1 keeps the example wrapper with `DEFAULT_SEG_URL` while moving the reusable generalized function into `em_erl`.

New findings: none.

The plan is scoped, behavior-preserving, and executable as written based on the provided artifacts.

READY: yes