1. **[major] The plan does not establish one shared cube playbook.** Proposed Changes §2–3 independently lifts `j0126_full` and selects it for j0126; no composition or selection mechanism makes j0126 use `cube_decode`. Specify how both existing datasets run the canonical cube playbook while preserving j0126’s legacy full-pipeline commands. Simply adding smoke/guard/score to that legacy invocation would contradict the command-equivalence gate.

2. **[major] Excluding mask extraction leaves “dataset #3 needs config only” unresolved.** Different storage formats and sharding justify retaining separate implementation strategies, but the plan supplies no shared interface consuming mask paths, polarities, ratios, frame, and border settings. Define that configurable interface and its use by `cube_decode`. Preserve existing script paths and resume behavior through wrappers or adapters. The task excludes numerical changes, not behavior-preserving extraction.

3. **[major] Fitted-value enforcement has gaps.** Proposed Changes §4 validates only `cube_decode`, leaving `j0126_full` outside the protection. It also describes checking null values without clearly requiring absent keys to fail before `prepare_config` supplies defaults. Specify validation of all four required dataset values, before default insertion, on both cube execution routes. Test absent and null values separately.

4. **[major] The verification gate does not cover the main execution risks.** Verification §1 filters live dry-run output without establishing that every step emits a command; completed steps could leave behavior untested, and filesystem changes could invalidate comparisons. Command strings also cannot establish preservation of `prepare`, `prepare_fn`, input gating, or completion checks. Use deterministic fixtures covering pending/completed/skipped steps, compare commands and resolved submission options, and test preparation hooks, input handling, and both resource schemas. The proposed tests cover only part of j0126’s identified risk.

5. **[major] Config-only onboarding needs an integration gate and a validation-registration decision.** No test constructs dataset #3 using only `params.yaml` and `2_abiss.yaml`, then resolves every cube step. The supplied repository contract also requires custom workflow roots to be declared in `scripts/validate_tutorial_configs.py`; the plan does not explain how a new tutorial satisfies that requirement without Python changes. Specify discovery or registration that preserves strict validation, and test the synthetic third tutorial through both playbook resolution and tutorial validation.

**Risks and Questions, item 5:** Choose `connectomics/playbooks/`. These modules contain executable logic, so normal package imports and packaging support are preferable. Keep dependencies explicit: playbooks assemble runtime primitives; the engine receives the selected playbook. Also define how tutorial and script paths resolve—packaging the Python modules alone does not package those assets.

READY: no