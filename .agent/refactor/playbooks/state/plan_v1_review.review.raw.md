1. **[major] Previous finding 1 remains partly unresolved — Proposed Changes §1.** The registry shares step implementations, but each tutorial still owns the ordered playbook through `params.pipeline.steps`. Dataset #3 must repeat the cube sequence, and nothing requires its guard or score stages. Define the canonical cube sequence once in `cube_decode.py`, with an explicit shared selection/composition mechanism that preserves j0126’s legacy commands.

2. **[major] Previous finding 2 is explicitly deferred in part — Proposed Changes §2; Risks 3–4.** The declarative mask interface excludes j0126’s existing strategy, and the plan narrows the config-only requirement without authorization. The fallback to postpone the shared spec directly contradicts “none are deferred.” Keep the existing resumable implementation and script paths, but expose both strategies through the common configurable interface. Put reusable mask computation in an importable package with thin script wrappers; introducing another substantive implementation under `scripts/` repeats the ownership problem identified by the task.

3. **[major] Previous finding 4 remains incomplete — Verification 1, 5; Risk 1.** The deterministic fixtures exercise the engine, while the plan explicitly leaves actual j0126 step definitions to manual review. The live comparison remains filesystem-dependent and is no longer a mandatory equality gate. Consequently, neither test establishes that the migrated tutorial builders preserve commands, preparation hooks, input paths, or completion predicates. Capture deterministic baseline behavior from both existing drivers and compare their replacement builders under controlled pending/completed/skipped states, including exact submission options.

4. **[minor] Previous finding 5 needs a stronger integration assertion — Verification 3.** The synthetic tutorial test is a useful addition, and the existing `abiss_chunk` root adequately answers registration on the supplied account. However, require the test to resolve the complete canonical cube sequence, assert that commands use the third tutorial’s supplied paths, and exercise the actual tutorial validator over both YAML files. Merely resolving a self-selected step list and checking registered-root membership could miss the onboarding failure the test is intended to prevent.

Previous finding 3 is addressed as specified: all four keys are checked for absence and null before default insertion on both routes. The package location and repository-root asset-resolution decisions also answer the previous open question. The claim that all five findings are fully closed is therefore unsupported.

READY: no